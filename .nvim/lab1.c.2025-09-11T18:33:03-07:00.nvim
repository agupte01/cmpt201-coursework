#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {

  char *text = NULL;
  size_t n = 0;
  printf("Please enter some text: ");
  if (getline(&line, &n, stdin) != -1) {
    printf("Token : \n");
    char *a = NULL;
    char *sptr = NULL;
    char *str = text;
    while ((a = strtok_r(str, " ", &sptr))) {
      printf("%s\n", a);
      str = NULL;
    }
  } else {
    printf("Failed to read line.\n");
  }
  free(text);
  return 0;
}
